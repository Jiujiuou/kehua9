## 可话年度报告（前端版）——章节内容说明

> 范围：仅描述 `src/components/Basic/AnnualReportCard/` 在 `AnnualReportCard/index.jsx` 中**实际渲染的页面**与各章内容（不包含 `mock/`）。

### 页面顺序（`totalPages = 9`）
- **Page 0**：`NicknameInputPage`（输入昵称开场页）
- **Page 1**：`Chapter1`（开场与起点）
- **Page 2**：`Chapter2`（个人词典：词云）
- **Page 3**：`Chapter3`（表达光谱）
- **Page 4**：`Chapter4`（情绪全景）
- **Page 5**：`Chapter5`（数字背后）
- **Page 6**：`Chapter6`（时光流转：24 小时活跃轨迹）
- **Page 7**：`Chapter7`（点亮的日子：年度日历）
- **Page 8**：`FinalChapter`（旅程的终点：结尾页）

### 报告容器（`AnnualReportCard`）做了什么
- **展示形态**：弹层/遮罩 + 背景图（随 `data-theme` 切换明暗背景）。
- **分页切换**：左右箭头按钮 + 键盘左右键切换，`ESC` 关闭。
- **输入保护**：在 Page 0 没有输入昵称时，禁止进入下一页。
- **动态卡片预览**：章节内可通过回调把某条动态交给 `CardPreview` 打开（当前主要由 `Chapter3` 触发）。

---

### Page 0：昵称输入（`NicknameInputPage`）
- **开场文案**：用打字机效果逐字呈现固定引导文案（`FULL_TEXT`）。
- **昵称输入**：显示输入框与“开始回忆之旅”按钮；支持 `Enter` 提交。
- **进入回忆**：昵称非空时触发 `onStartMemory`，由容器推进到下一页。

### Chapter1：开场与回忆起点（`Chapter1`）
- **数据范围**：先按 `ANNUAL_REPORT_END_DATE` 过滤动态（截止日当天 23:59:59.999 之前）。
- **核心统计**：
  - **记录条数**：过滤后的动态条数。
  - **陪伴天数**：从“第一条动态的日期”到固定的 `ANNUAL_REPORT_END_DATE` 的天数差，按 
    \[\left\lfloor \frac{end - start}{86400000} \right\rfloor + 1\] 计算，并 `Math.max(1, ...)` 保底。
- **叙事与展示**：
  - **欢迎语 + 昵称**：渐显动画。
  - **数字增长动画**：`displayTotalDays` / `displayTotalCount` 从 0 增长到目标值。
  - **温情描述文案**：根据天数与条数生成一句话（把天数粗略映射成“几年多/近几年”，并把条数格式化成 `xxx+`）。
  - **第一条与最后一条动态**：展示过滤后最早与最晚动态（文本截断）。
  - **时间情绪描述**：基于节日/季节/时段/星期生成“那是怎样的一天”的旁白（如“周末在望的周五傍晚”等）。

### Chapter2：个人词典（`Chapter2` + `WordCloud`）
- **文本提取**：从 `dynamics` 里提取可用于词云的文本（`extractTextFromDynamics`）。
- **Worker 分词与词频**：
  - 使用 `wordCloud.worker.js` 在 Web Worker 中进行简化分词（滑动窗口提取 2-3 字词 + 2-3 字母英文词）。
  - 大量停用词/停用字过滤，额外的“碎片词”过滤与后处理合并（例如把可合并的 2 字片段合成 3 字词）。
  - 最终返回高频词列表（最多前 100）。
- **词云渲染**：
  - 使用 `wordcloud` 库在 `<canvas>` 渲染，按词频映射字体大小与颜色（`getColorByFrequency`）。
  - 对容器 `ResizeObserver`：尺寸变化时仅重绘，不重新跑 Worker。
- **交互**：
  - **Hover 词语**：高亮展示该词，并从原动态中抽取最多 3 条“包含该词”的文本片段；默认展示词频最高的词。

### Chapter3：表达光谱（`Chapter3`）
- **两极展示**：
  - 从所有带文本的动态中找出 **最短** 与 **最长** 两条，使用 `DynamicCard` 展示。
  - 支持图/视频预览（`ImagePreview` / `VideoPreview`），并支持点击内容触发外部 `CardPreview`（由容器承接）。
- **视角倾向分析**：
  - 基于 `PERSPECTIVE_LEXICON` 对文本片段做启发式打分，归类为：**向内看（intro）/ 向外看（outro）/ 记录（neutral）**。
  - 百分比分配使用“最大余数法”确保三类合计为 100%。
- **叙事输出**：
  - 生成一段“你与世界对话方式”的总结文案（多模板随机选择）。
  - 为每类视角提取最多 3 条“典型句子”作为例句展示。

### Chapter4：情绪全景（`Chapter4`）
- **情绪分析**：
  - 提取动态文本，调用 `analyzeEmotionsBatch`（批量分析）得到每条文本的主情绪。
  - 按 `EMOTION_CATEGORIES` 汇总各情绪的数量与占比，并按占比降序排列。
- **可视化呈现**：
  - 列表式“情绪光谱”：每种情绪显示颜色点、名称、进度条、百分比。
  - 额外展示“最常见情绪”的一句解释。
- **状态与节奏**：有“正在分析情绪...”加载态，并在分析后分批淡入展示。

### Chapter5：数字背后（`Chapter5`）
- **数据范围**：按 `ANNUAL_REPORT_END_DATE` 过滤动态。
- **统计内容**：
  - **动态类型统计**：纯文字 / 纯图片 / 纯视频 / 图文混合（以 `text/images/videos` 组合判定）。
  - **内容统计**：总字数、含文本动态数、平均字数、图片总数、视频总数。
  - **记录频率**：以“有记录的日期集合”计算活跃天数，再得到平均每天记录次数。
- **文案生成**：把统计结果拼装成多段 `{ type: main|normal, text }` 的叙事文案（主句 + 补充句）。
- **展示风格**：按章节主题做标题/副标题/段落淡入；文案区为普通排版（不做卡片背景/阴影）。

### Chapter6：时光流转（`Chapter6` + `HourlyActivityRiver`）
- **数据范围**：按 `ANNUAL_REPORT_END_DATE` 过滤动态。
- **时间分布统计**：
  - 统计 0-23 点每小时的记录次数（优先用 `dynamic.time`，否则用 `timestamp`）。
  - 找出最活跃小时（含占比），并统计深夜段（23:00-05:00）的记录次数与占比。
- **可视化**：
  - `HourlyActivityRiver` 用 SVG 渲染“24 小时活跃度河流图”，包含渐变填充、峰值标记与时间刻度。
- **叙事文案**：根据“最活跃小时”落入的时段（清晨/午后/深夜等）生成主文案，并在需要时补充深夜记录描述。

### Chapter7：点亮的日子（`Chapter7` + `YearCalendar`）
- **数据范围**：按 `ANNUAL_REPORT_END_DATE` 过滤动态。
- **年度口径**：默认以 `ANNUAL_REPORT_END_DATE` 的年份作为展示年份（例如 2025）。
- **日历统计**：
  - **点亮天数**：按日期去重后的活跃日期数量。
  - **最长连续天数**：用 UTC 天粒度计算相邻日期差是否为 1 天，得到最长 streak。
  - **最常记录的星期**：按本地时区统计周几最常出现及占比。
- **可视化**：
  - `YearCalendar` 绘制 12 个月的网格日历，按“是否有记录”点亮。
  - 点亮有逐步动画（按活跃日数量在约 8 秒内逐个点亮）。
- **叙事文案**：输出“这一年点亮了多少天、最长连续记录、最常在周几记录”等温情总结。

### FinalChapter：旅程的终点（`FinalChapter`）
- **全量数据要求**：结尾页在容器层使用 `allDynamics` 传入，保证始终基于“所有年份”的全量动态。
- **首条动态日期**：在全量 `dynamics` 中找最早的日期（优先 `dynamic.date`，否则从 `timestamp` 格式化）。
- **相伴天数**：用 UTC 天粒度计算“首条动态日期”到 `ANNUAL_REPORT_END_DATE` 的包含首尾天数：
  \[\left\lfloor \frac{endUTC - startUTC}{86400000} \right\rfloor + 1\]
- **结尾文案与结构**：展示“第一条动态日期 → 相伴至今（截止日）”的时间轴块、天数大数字，以及收束式告别文案。

---

### 备注
- `EmotionRiver/` 目录提供了“情绪河流图”组件（Canvas 可视化 + 点选详情面板），但当前**未被 `AnnualReportCard/index.jsx` 接入到某一章**，属于可复用能力储备。

7) “字数的季节变化”
内容：按月统计平均字数/总字数，形成“表达强度曲线”。
温度：春天轻盈、秋天厚重、冬天更内敛。

8) “相册里的温度”
内容：图片占比与视频占比，展示“你更习惯用画面还是文字留住记忆”。
温度：画面慢但久，文字轻但真。

9) “一句话写给过去的自己”
内容：用数据自动生成一封 5–7 句的小信（天数、记录次数、活跃时段、最长连续天数等）。
温度：结尾语气温柔、克制、像朋友。

12) “为自己留下的一句话”
内容：允许用户在报告结尾输入一句话（localStorage 保存），当作真正的“最后一页”。
温度：交给用户写下自己的收束。